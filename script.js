// template
document.getElementById('contact-form').addEventListener('submit', function(event) {
    event.preventDefault();
    // Send message to server or handle form submission
    console.log('Form submitted');
});
